# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'FaceAuthSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FaceAuthSample
  pod 'SwiftLint'
  pod 'Moya/RxSwift'
  pod 'RxSwift'
  pod 'SVProgressHUD'

  target 'FaceAuthSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FaceAuthSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
